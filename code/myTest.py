import numpy as np
from scipy import sparse
import collections
import torch
import torch.nn as nn

nums = [11, 22, 33, 44, 55]
nums = set(nums)
for index, number in enumerate(nums):
    print(index, number)

print()